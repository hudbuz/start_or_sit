

  function CompareController($scope, $state, IndexService, PlayerService){
    var compare = this
    compare.name = 'hudson'

  }
  angular
    .module('app')
    .controller('CompareController', CompareController)
